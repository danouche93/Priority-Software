# Base URL of the SoundSearch.Api backend.
# Only needed when running the frontend on its own dev server (npm run dev),
# separate from the backend. Matches `dotnet run --urls http://localhost:5193`.
# In production the backend serves the frontend itself, so this isn't needed.
VITE_API_BASE_URL=http://localhost:5193
